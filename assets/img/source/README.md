# Mirrored site imagery (from pa-expert.com)

Drop the current site's backgrounds and pictures here (hero/background images,
Jason Purinton's headshot, section graphics, etc.), then tell Claude to wire
them in.

Include a short note (or keep original filenames) so each image's **usage** is
clear — e.g. `hero-bg.jpg` (home hero background), `jason-purinton.jpg`
(about/headshot), `cta-bg.jpg`, etc.

The container is blocked from fetching pa-expert.com directly, so these must be
added to the repo (paste isn't possible for binary images — use GitHub web
upload or `git add/commit/push`, or run the scraper and commit what it
downloads).
