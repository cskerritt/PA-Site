# Purinton Analytics (pa-expert.com) — Full Site Overhaul Design

**Date:** 2026-06-23
**Repo:** `cskerritt/PA-Site` (branch `claude/pa-expert-website-redesign-8wb1bh`)
**Status:** Design approved (direction + scope + form handler); pending user spec review.

---

## 1. Goal

Take the existing static redesign of **pa-expert.com** from "plausible draft" to
"accurate, on-brand, launch-ready" in a single comprehensive pass across four
workstreams: **(1) content & accuracy**, **(2) brand & visual refresh**,
**(3) new pages/features**, **(4) launch readiness**.

The site remains a **zero-runtime static site** generated from a single source of
truth, `build.py`, and hosted on **GitHub Pages** (`CNAME = pa-expert.com`).

---

## 2. Current State (as found)

- Static site, all HTML committed, generated by `build.py` (~104 KB single file:
  `SITE`, `NAV`, `SERVICES`, `PRACTICE_AREAS`, `DETAILS`, `STATES`, city helpers).
- Design system in `assets/css/style.css` (447 lines): navy **+ gold**, Fraunces
  serif headings + Inter body, hero/stat-band/card-grid/feature-grid/CTA/FAQ/
  footer components, scroll-reveal animations, reduced-motion support, responsive.
- Pages: Home, About, 5 Services, 4 Practice Areas, Locations (7 states / 319+
  cities), Contact, Privacy, 404.
- **Known gaps (README):** contact form posts to a Formspree placeholder; email is
  a guessed `info@pa-expert.com`; OG image is SVG only (no 1200×630 PNG); business
  facts "reconstructed from public listings" and flagged for accuracy review.

---

## 3. Brand System (from supplied brand kit + logo)

Source: `~/Downloads/pa-expert-brand-assets/` + `~/Downloads/purinton-analytics-logo.svg`.
The real brand is navy **+ orange + teal** (current site uses gold — to be replaced).

### 3.1 Palette (CSS tokens to set in `:root`)
| Role | Hex | Notes |
|---|---|---|
| Hero deep navy | `#012262` | primary dark / hero base |
| Hero cobalt | `#00256A` | gradient partner to navy |
| Primary accent (orange) | `#FE8F36` | CTAs, accents — **replaces gold** |
| Orange gradient | `#ff8f36 → #f47108` | buttons, logo mark, emphasis |
| Secondary accent (teal) | `#479B83` | dividers, secondary highlights, wave |
| White | `#FFFFFF` | foreground on dark |
| Ink / slate neutrals | keep existing | body text, borders, alt backgrounds |

Keep the existing neutral ramp (`--ink`, `--slate`, `--line`, `--bg-alt`, etc.);
only the brand hues change. WCAG AA contrast must be re-verified for orange-on-navy
and orange button text (use dark text on orange fills, as today).

### 3.2 Logo
`purinton-analytics-logo.svg` (viewBox 187×66): orange gradient monogram mark +
**white** "Purinton Analytics" wordmark (paths, not live text).
- **Dark surfaces** (hero, footer): use as-is (white wordmark).
- **Light header:** create a navy-wordmark variant (recolor `#fff` paths → `--navy`)
  so the wordmark is legible. Replace the current text "PA" `.brand-mark`.
- Derive **favicon** (mark only) and **apple-touch-icon** from the mark.

### 3.3 Typography
Keep **Fraunces** (serif headings) + **Inter** (body). The brand kit could not
recover the live site's fonts; this pairing fits the forensic/clinical/legal tone.
(Low-risk; revisit only if user prefers a match.)

### 3.4 Imagery inventory (copy into `assets/img/brand/`, keep WebP)
| Asset | Use |
|---|---|
| `Jason-Purinton-2025-BW.webp` (655×450) | Home hero card portrait |
| `about-Jason-Purinton-Informal-BW.webp` (850×1000) | About bio portrait |
| `home-hero-PA-header-image.webp` (1600×725) | Home hero illustration (motion city) |
| `line-vector.webp` (1600×654, teal) | Section accent / divider art |
| `contact-{Kansas-City,St-Louis,Denver,Chicago}-Skyline.webp` (800×533) | Metro office pages |
| 11 `association-*-logo.webp` (110px tall, monochrome) | Credential/affiliation strip |

Association logos: SEAK, IARP, ABVE, ICHCC, CRCC, AREA, NBCC, Psychology Today,
ALM/Law.com, JurisPro. Render the strip in a muted/monochrome treatment (these are
third-party marks shown as credibility signals, not endorsements — label honestly,
e.g. "Memberships & Listings").

---

## 4. Content & Accuracy Corrections (Workstream 1)

Authoritative source: his **18-page CV** (read in full during planning). Where the
live website and the CV conflict, the **CV is the conservative baseline** and the
conflict is flagged in §4.1 for the user to confirm (the CV may predate live-site
updates). Forensic context: every published credential is litigation-exposed, so
**no claim ships that the CV doesn't support unless the user confirms it.**

CV-verified facts to bake into `build.py`:

1. **Credential string (CV header):** `LPC, CRC, CVE, ABVE/F, IPEC`. Full certifications: Licensed Professional Counselor (MO), National Certified Counselor (NBCC), Certified Rehabilitation Counselor (CRCC), Certified Vocational Evaluator (CRCC), ABVE Fellow & Board Certified (2025), International Psychometric Evaluator–Certified / IPEC (2025), Forensic Vocational Expert (AREA, F0033), Certified Rehabilitation Provider (MO), Qualified Rehabilitation Professional (KS), Qualified Vocational Rehabilitation Counselor/Job Placement Specialist (NE), Certified Trauma Professional, Certified Critical Incident Stress Debriefer, Registered Nurse (MO). **CLCP is NOT on the CV — do not publish it unless confirmed (§4.1).**
2. **Firm history:** Vocational Consultant at **Purinton Analytics, LLC (formerly Vocational Pros, LLC)** since **2018**; based Kansas City, MO; rebranded to Purinton Analytics **2024**.
3. **Experience headline:** *"Testified in over 3,000 Social Security disability hearings across the United States."* Use this exact framing (SSA hearings, not generic "evaluations"). Forensic VR expert witness for SSA Office of Disability Adjudication & Review; uses DOT, O*NET, Occupational Requirements Survey.
4. **Positioning:** uniquely also trained/licensed as a mental-health counselor + RN (clinical depth); opinions grounded in objective methodology, psychometric testing, labor-market analysis, and current vocational-rehabilitation principles. (The mental-health-counselor framing is from the live site; the LPC/RN basis is CV-verified.)
5. **Leadership (CV-exact):** American Rehabilitation Economics Association — **Board of Directors, President-Elect (2025)**. IARP — Board of Directors, Forensic Section Representative (2022–2024). Medical Missions Foundation — President of the Board (2020–2021). **ABVE: Fellow (2025), member — NOT a board role per the CV.**
6. **Education (CV-exact):** **MS, Rehabilitation Counseling** (emphasis Vocational Rehabilitation), Emporia State University, 2018–2019, Phi Kappa Phi; **BSN, Registered Nurse**, MidAmerica Nazarene University, 2016–2017; **EMT**, Johnson County Community College, 2014; **BS, Business** (emphasis Communication Studies), University of Kansas, 1993–1997.
7. **Licensure reach:** state voc-rehab/workers'-comp qualifications in **Missouri, Kansas, and Nebraska**; testimony nationwide.
8. **Offices (CV):** **Denver, Kansas City, St. Louis, Chicago** — listed as **cities only; the CV gives no street addresses.**
9. **Contact (CV):** phone **877-882-9778**; email **jason@pa-expert.com**; LinkedIn **linkedin.com/in/pa-expert**; Facebook **facebook.com/Purinton.Analytics**; X **@PurintonExpert**. (Generated site's `info@` email, `/in/purintonanalytics` LinkedIn, and `(913) 484-4346` mobile are NOT on the CV — correct/confirm.)
10. **Practice areas (4 → ~13):** Personal Injury, Workers' Compensation, Employment Litigation, Family Law, Medical Malpractice, Motor Vehicle/Trucking, Premises Liability, Product Liability, Labor Law (incl. FELA/railroad — CV-supported), Long-Term Disability, Social Security Disability (CV-supported), Wrongful Death, Veteran's Disability. *(PI, workers' comp, family law, SSDI, FELA/railroad, long-haul trucking are CV-evidenced; the rest derive from the live site's own marketing — acceptable for service pages, grounded in his actual methodology.)*

### 4.1 Discrepancies to confirm with the user (live site vs. CV)
The site ships the **CV-conservative** version of each until confirmed:
| Claim | Live site says | CV says | Ship until confirmed |
|---|---|---|---|
| CLCP credential | Lists **CLCP** | Not present | **Omit CLCP** |
| AREA role | "President" | "President-Elect (2025)" | "Board of Directors / President-Elect, AREA" |
| ABVE board | "Board of Directors" | Fellow only | "Fellow, ABVE" |
| Mobile phone | `(913) 484-4346` on draft | Not on CV | Office line only |
| St. Louis street address | `231 S. Bemiston Ave, Ste 800` | Cities only | No street address / "by appointment" |

**Accuracy guardrail:** Do **not** emit fabricated `PostalAddress`/`GeoCoordinates`
for any office without a confirmed street address. Offices without a verified
address are presented as "meetings by appointment" (no fake schema). See §7.

---

## 5. Information Architecture

### 5.1 Existing pages (content-corrected + re-skinned)
Home, About, Services overview + 5 service pages, Practice Areas overview,
Locations (7 states / 319+ cities), Contact, Privacy, 404.

### 5.2 New pages (all approved)
- **9 new practice-area pages** — one per added area (§4 item 8). Each: page-hero,
  scope/what-we-evaluate, relevant services, FAQ, CTA, `Service`/`FAQPage`/
  `BreadcrumbList` schema. Update `PRACTICE_AREAS`, `DETAILS`, nav/overview,
  `sitemap.xml`, `llms.txt`.
- **4 metro office pages** — `/offices/{kansas-city,st-louis,denver,chicago}/`
  with skyline imagery, metro-specific copy, courts/venues served, and
  `LocalBusiness` schema **only where a real address exists** (else appointment copy).
  Distinct from the programmatic state/city SEO pages (those stay).
- **Credentials & Affiliations** — `/credentials/` (or About section): full
  credential detail, memberships, leadership roles, and the association-logo strip.
- **Insights/Articles** — `/insights/` index + article template (GEO/SEO surface).
  Seed with a small number of factual, citable articles; `Article`/`BreadcrumbList`
  schema; wired into nav, sitemap, llms.txt. (Content seeds confirmed at build time.)

### 5.3 Navigation
Add the new sections sensibly (e.g. Services, Practice Areas, Offices/Locations,
Insights, About, Contact). Keep mobile nav working; avoid overcrowding — group if
needed (Locations vs. Offices distinction must be clear).

---

## 6. Architecture & Build Approach

- **Single source of truth stays `build.py`.** Extend data structures
  (`SITE`, `PRACTICE_AREAS`, `DETAILS`, plus new `OFFICES`, `ARTICLES`,
  `AFFILIATIONS`) rather than hand-editing generated HTML.
- **Refactor watch:** `build.py` is already large. As we add pages, keep page-type
  builders as focused functions and data as data; if a section grows unwieldy,
  split into a small module (e.g. `content.py` for data, `build.py` for rendering)
  — only if it genuinely improves clarity, no gratuitous refactor.
- **Asset pipeline:** copy brand assets into `assets/img/brand/`; reference WebP
  with appropriate `width`/`height`/`alt`/`loading="lazy"`. Generate favicon +
  OG PNG (1200×630) into `assets/img/`.
- **Regeneration:** `python3 build.py` rebuilds all HTML + `sitemap.xml`,
  `robots.txt`, `llms.txt`, `site.webmanifest`, `CNAME`. `llms.txt` and sitemap
  must reflect every new page.
- **SEO/GEO consistency:** every new page gets title/description/canonical/OG +
  JSON-LD; AI-crawler allowances in `robots.txt` preserved.

---

## 7. Launch Readiness (Workstream 4) + Open Inputs Needed

Build everything else now; these user-supplied values are collected before/at deploy:

1. **Web3Forms access key** — chosen handler; I wire the form, user pastes the key.
2. **Office addresses** — for each of KC / St. Louis / Denver / Chicago: real
   street address (→ full `LocalBusiness` schema) **or** "by appointment"
   (→ no fabricated address). CV gives cities only.
3. **§4.1 credential confirmations** — CLCP (yes/no), AREA title (President vs.
   President-Elect), ABVE board (yes/no), mobile phone (publish or not).
4. **Deploy/DNS** — confirm GitHub Pages as host and whether to cut `pa-expert.com`
   DNS over now or stage on the Pages URL first.

**Resolved from CV (no longer open):** email `jason@pa-expert.com`; office phone
`877-882-9778`; LinkedIn `/in/pa-expert`; Facebook `/Purinton.Analytics`; X `@PurintonExpert`.

Launch tasks: Web3Forms wiring + spam honeypot, real email throughout, favicon +
OG PNG, rebuild, local verification on preview server (`:8123`), validate
`sitemap.xml`/`robots.txt`/`llms.txt`, then deploy.

---

## 8. Testing / Verification

- `python3 build.py` runs clean; generated HTML diff reviewed.
- Local preview (`:8123`): visual check of Home, About, a service, a new
  practice-area page, a metro office page, Credentials, an Insights article,
  Contact — at desktop + mobile widths, light + dark-emulation.
- A11y: skip link, focus-visible, color contrast (orange/navy), reduced-motion,
  alt text on all new imagery, logo `aria-label`.
- SEO: titles/canonicals unique; JSON-LD validates; sitemap/llms.txt include all
  pages; no fabricated address schema.
- Contact form: submits to Web3Forms (once key provided); success/error states.

---

## 9. Out of Scope (YAGNI)

- No CMS / dynamic backend / build framework — stays static.
- No re-architecture of the existing 7-state / 319-city programmatic system
  (kept as-is; only re-skinned).
- No new fonts unless requested.
- No paid analytics/tracking added.
- Blog/insights seeded small, not a full content-marketing program.

---

## 10. Workstream Sequence (for the implementation plan)

1. **Brand foundation** — palette tokens, logo integration, asset import, favicon/OG.
2. **Content accuracy** — CV deep-read + correct all `build.py` data; re-skin existing pages fall out of (1).
3. **New pages** — practice-area pages → metro office pages → credentials → insights.
4. **Launch** — Web3Forms, real email/addresses, rebuild, verify, deploy.

Each step ends with a clean `build.py` run and a local preview check.
